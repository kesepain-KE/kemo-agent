kemo-agent
[框架结构]
provider:LLM接口，只负责连接外部API，支持openai的chat模式和kemo网关专用模式，外接多模态能力(图像识别/生成、语音识别/合成、视频生成/识别、语音生语音)。token统计通过kemo网关精确计算，无网关时使用残血估量统计
run:核心模块，Agent运行核心(上下文窗口检测、子智能体调用、工具调用、prompt拼接、权限管理、多用户管理、拓展能力管理、全局感知管理、消息路由管理)。支持并行处理多个用户上下文，共享记忆但隔离对话窗口
plugins:智能体插件(可被更新覆盖)，带版本号，支持按插件粒度更新，优先级低于用户skill和共享skill，根目录有插件注册机制。插件与核心模块的API兼容性由开发者自行维护
cron:定时任务核心模块，检测用户定时任务达到执行时间后触发子代理agents\time_plan执行，传入cli.py
users:多用户目录，含_template模板，内部包含avatar、download、expand、file_upload、history、improve、knowledge、task_cron、task_plan、user_skills、user_config.json、user_soul.md、memory_temporary_important.md
message:外部消息路由核心(可被更新覆盖)，默认cli入口，独立上下文窗口，有独立的消息注册机制，每个消息入口以文件夹形式区分，各自具有限制性工具调用权限。同一用户的多个消息入口共享记忆但隔离上下文(如QQ和Web是同一个智能体)
global_expand:全局拓展核心(可被更新覆盖)，外接kemo-graph(外部知识图谱，帮主智能体实现RAG+知识图谱检索)，部分拓展需配合工具使用
global_sense:全局感知核心(可被更新覆盖)，根目录有注册机制，外接电脑传感器数据/使用情况/硬件数据等，默认注册到prompt某一区间。项目初始安装无传感器数据，仅提供注册机制，数据采集由外部适配器负责，智能体采集到什么注入什么
global_knowledge:全局共享知识库(可被更新覆盖)，可结合kemo-graph增强检索，存放项目运行说明、操作说明、实现原理
shared_skills:多用户共享技能(除运行文件外不被更新覆盖)，优先级高于plugins，低于用户skills
shared_expand:多用户共享拓展(除运行文件外不被更新覆盖)
shared_knowledge:多用户共享知识库(不可被更新覆盖)，可结合kemo-graph增强检索
config:全局配置数据目录，含global_soul.md全局人格文件和全局配置文件。全局配置文件包含子代理触发频率(n1-n8)、碎片数量上限、碎片字符上限、专用模型配置等可调参数
agents:子代理集群，最终prompt拼接前注入职责说明，每个子代理以独立目录+说明文件形式存在。子代理没有上下文权限，但有部分工具调用能力，由调用方传入文本参数(含用户标识)
web:智能体网页端(web运行目录)

[入口文件]
cli.py:命令行入口，被定时任务和消息路由调用
start_web.py:网页端启动入口
update.py:更新脚本(覆盖plugins/全局模块，不覆盖users和shared_*)
agents.md:智能体行为手册

[system prompt拼接顺序]
固定拼接顺序，不可调换。冲突规则:当前用户指令 > user_soul.md > agents.md > global_soul.md

1.global_soul.md:全局人格基座，所有用户共享的行为底线和硬性规则
2.user_soul.md:用户个性化人格覆盖，决定语气/角色/偏好
3.agents.md:智能体自我操作手册，工作规则和目录规范
4.memory_temporary_important.md:热画像层，仅次于永久记忆的用户画像
5.记忆碎片各层(7天/30天/180天/永久):每碎片有字符上限，每层有碎片数量上限
6.工具说明&子代理职责:注册的工具列表和子代理触发规则
7.知识库(按需):用户主动提及时注入，采用文件索引方式调用
8.对话历史:前3轮全量注入(文本+工具调用+思考)，3轮外工具调用限制<200字符

[system prompt膨胀控制]
记忆碎片自带字符限制属性，控制注入预算:
-各层碎片数量上限:7天≤15个、30天≤20个、180天≤25个、永久≤30个
-每碎片最大字符:临时层200字符、永久层300字符
-热画像单文件≤1500字符
-超限时由子代理自动压缩旧碎片或合并相似碎片
-专用模型配置:高频操作(记忆巡检/碎片去重/上下文压缩)可配置廉价模型降成本

[部分架构精细]
<users目录>
users
|—_template(用户模板，新增用户时复制此目录)
|—用户名
     |—avatar(用户头像)
     |—download(智能体生成的文件存放处)
     |—file_upload(用户上传的文件存放处)
     |—expand(用户拓展文件目录)
     |—knowledge(用户知识库，纯文件形式，索引支持。AI可二次整理但用户自行承担审核责任，可选加审核机制)
     |—history(以时间戳命名的对话窗口目录，每个窗口下四个文件)
     |    |—{时间戳}/
     |    |    |—text.json(用户和智能体文本对话)
     |    |    |—think.json(思考过程数据)
     |    |    |—tool.json(工具调用日志)
     |    |    `—data.json(窗口元数据)
     |—improve(记忆目录，按时间分层)
     |    |—seven_days(7天临时记忆)
     |    |    `—data.json
     |    |—one_month(30天临时记忆)
     |    |    `—data.json
     |    |—half_year(180天临时记忆)
     |    |    `—data.json
     |    `—permanent(永久记忆)
     |         `—data.json
     |—task_plan(任务计划)
     |—task_cron(定时任务)
     |—user_skills(用户技能，标准技能结构)
     |    |—agent_create(智能体被动创建的技能，180天工作型记忆自动转化)
     |    `—user_create(用户主动要求创建的技能，优先级>agent_create)
     |—user_config.json(用户配置文件，含专用模型配置、碎片上限、子代理频率等可调参数)
     |—user_soul.md(智能体个性化人格)
     `—memory_temporary_important.md(临时重要记忆，仅次永久记忆的热画像层)

<agents目录>
agents
|—context_manage(上下文管理)
|    `—上下文管理.txt
|—token_condense(token压缩)
|    `—token压缩.txt
|—self_improve(自我改进/记忆管理)
|    `—自我改进.txt
|—memory_temporary_important(临时重要记忆巡检)
|    `—临时重要记忆.txt
|—task_plan(任务计划)
|    `—任务计划.txt
`—time_plan(定时任务)
     `—定时任务.txt

<improve记忆分层>
按时间分层，不按类型分层。记忆碎片和自我改进规则共存于data.json，以碎片名称区分类型。
-seven_days → one_month → half_year → permanent
-升级线路:7天>30天>180天>永久
-各档权重:7天默认4权(每天降权1次)、30天默认20权、180天默认120权
-每天最多加权1次，触发条件:对话文本中提及或讨论相关记忆碎片(被引用或存在语义相关性)
-用户一周无对话，所有碎片持续降权
-权重降为0则删除，达到升级天数且权重不为0则升级档次
-180天达到升级条件后，升级为永久记忆；如果是工作型记忆(LLM判断)，自动创建为用户技能
-碎片去重:关键词索引配合全量注入一次对比，去重频率由配置文件控制，可配置专用廉价模型执行
-低质量碎片大概率活不过7天(权重持续衰减)，依赖提示词工程控制碎片提取质量

<子代理执行机制>
-子代理为后台异步执行(用户基本无感)，采用单线程任务队列
-上一个任务执行完成后才切换下一个任务，避免并发读写冲突
-子代理没有上下文权限但可接收调用方注入的文本参数(含用户标识)
-文件写入采用原子操作:先写临时文件，写完后os.rename覆盖目标文件
-四文件一致性:单窗口写入需四个文件全部就绪才标记完整；读取时若发现不完整则注入空值或丢失标记

<memory_temporary_important热记忆>
-比临时记忆文件中梳理出的重要记忆碎片，重要程度仅次于永久记忆
-反映用户日常使用最直接的用户画像，放用户根目录与user_soul同级
-每隔n6小时巡检一次：读取临时记忆→发现重要碎片(伴侣/学校/亲人/性格/情感)→更新此文件→检查永久记忆去重
-每天n7时间点统计字符数量，超限则压缩；检查记忆碎片尝试合并

<expand目录>
expand
|—kemo-graph连接目录(外部知识图谱，对外)

[history会话窗口粒度]
一个时间戳目录=一个对话窗口(从对话开始到上下文压缩/清空)
窗口内包含多轮一问一答，压缩时整个窗口的四文件一起处理

<四个文件职责>
-text.json: 用户消息+智能体回复文本
-think.json: 思考过程(只在本轮注入llm，前三轮外的think写入"已思考")
-tool.json: 工具调用输入输出日志
-data.json: 窗口元数据(时间、轮数、token统计)

============================================================================================================================================================================

1.长期记忆、临时重要记忆、临时记忆、上下文记忆、知识库，这几个永久开启（实现记忆机制、简单知识库、上下文记忆）
2.RAG+知识图谱为外部拓展能力（项目kemo-graph，可通过global_expand接入），RAG处理大型批量文档，知识图谱链接逻辑节点（用户从外部自己整理的信息），两者均为开关控制
3.单独的历史对话搜索工具
4.自主产生技能能力

长期记忆（需要用户主动要求记忆）
永久注入提示词的数据
临时重要记忆（智能体被动记忆，从临时记忆中梳理的重要碎片）
仅次于永久记忆的热画像层，每隔n6小时巡检更新，每天定时压缩整理
临时记忆（智能体被动记忆）
（7天、30天、180天三层时间档，按时间分层不按类型分层）
（记忆碎片+自我改进规则共存于data.json，以碎片名称区分类型）
（7天默认4权重，每天降权1次，每天最多加权1次。权重为0则删除）
（天数达到且权重不为0则升级：7天→30天(默认20权重)、30天→180天(默认120权重)）
（180天达到升级条件，升级为永久记忆；如果是工作型记忆，自动创建为用户技能）
（token压缩触发、上下文对话数压缩触发、定时触发，三种触发方式由不同子代理负责）
（子代理self_improve读取临时记忆，判断是否更新/加权/升级/创建碎片）
上下文记忆
（用户输入+模型整理输出=一轮对话）
（思考过程只在本轮回复内注入llm，前三轮外对话的思考过程注入"已思考"）
（前3轮对话的工具调用全量注入：思考过程、用户文本、输出文本、工具调用）
（前三轮以外的工具调用日志输入和输出限制注入字符数量<200字符>）
（子代理context_manage后台静默运行，对前三轮每轮工具调用做语义摘要，摘要限制字符数量）
知识库
（用户主动提及时，模型二次整理知识库，默认整理成用户知识库）
（采用文件索引方式调用：高价值信息、高精度信息、高准确率信息、自我配置文档）
（AI整理知识库时用户自行承担审核责任，可配置审核机制开关）
RAG+知识图谱（开关控制，外部拓展，项目kemo-graph）
（大型、重型文档<markdown、word、pdf、ppt等>）
（专用向量模型，建议不随意更换）
（文档用户自己更新）
（用户从外部引入自己的知识图谱或知识）
（两者一起打开时，先执行知识图谱，再执行RAG）
历史对话记忆：
（专门工具搜索历史对话聊天记录，只能搜用户消息和agent消息，不能搜工具调用和思考过程）
（最低级别的记忆检索方案，用户主动提及触发）
自主产生技能：
（临时记忆更新时，如果发现180天临时记忆相吻合，审阅用户目录内自创技能和相似技能，新建或更新技能，此技能存活机制为永久）

[子代理职责]
context_manage(上下文管理):
  -工具日志压缩：最近n1轮外的对话，总结工具调用日志为简短摘要(保留关键思考/数据/调用)，替换原日志，思考日志替换为"已思考"
  -上下文压缩：对话轮数达到n2轮时，触发压缩至n3轮，截取n2-n3轮文本对话，调self_improve总结，拼接回上下文

token_condense(token压缩):
  -token限制触发：截取旧数据n4*(1-n5)的token量对话(不切断完整对话轮)，调self_improve总结，拼接回上下文
  -API的token限制触发：读取当前上下文token量，截取当前token*(1-n5)的对话数据，调self_improve总结，拼接

self_improve(自我改进/记忆管理):
  -上下文压缩/token压缩回调：接收对话数据(调用方注入)，提取记忆碎片关键词，检索已有临时记忆→命中则更新/加权，未命中则在7天区创建新碎片
  -记忆升级：临时记忆达到时间→权重不为0则升级(7天>30天>180天>永久)→180天升级时LLM判断是否为工作型记忆，是则查重后自动创建技能→权重为0则删除
  -主动记忆：用户主动要求记忆→整理关键词→检索永久记忆→命中则更新，未命中则永久创建

memory_temporary_important(临时重要记忆巡检):
  -每隔n6小时：读取临时记忆→发现重要碎片(伴侣/学校/亲人/性格/情感)→更新memory_temporary_important.md→检查永久记忆去重
  -每天n7时间点：检查字符数量超限则压缩，检查记忆碎片尝试合并

task_plan(任务计划):
  -创建：获取用户任务+工具+记忆+上下文→判断是否需要→需要则读n8(最大步骤数)→调用工具协调创建可执行计划→返回计划清单
  -编辑：获取计划数据+用户要求→修改→返回计划清单

time_plan(定时任务):
  -CRUD：获取用户要求→创建/编辑/删除定时任务
  -执行：定时任务到达执行时间→触发cli.py传递任务数据→等待结果→更新任务状态

[并行架构]
-同一用户的多个消息入口(QQ/Telegram/Web)共享记忆和知识库，但各自拥有独立的上下文窗口
-多用户同时对话时，run模块并行处理，各用户上下文互不干扰
-子代理任务队列为单线程串行执行，避免跨用户或同用户内的data.json并发读写冲突
-文件操作均采用临时文件+原子重命名，确保写入过程中崩溃不产生半成品
