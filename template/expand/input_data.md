# 拓展模块名称

> 自动采集时间：待首次运行 data_update.py 填充

## 数据

模块尚未运行。等待 RuntimeHost 自动刷新，或手动运行 `python data_update.py` 初始化。

> 本文件只保存适合进入 Prompt 的状态摘要或资源路径。完整 JSON、CSV、HTML、
> 图片、音视频和大型日志保存在模块内由实现自行选择的位置，再按需读取；
> 框架不规定内部目录名称或层级。

> 输出区分观测时间、数据来源、单位和未知状态，不写入执行指令；采集正常不代表本次操控成功。无真实数据时保持“尚未运行”，不得使用示例数值伪造状态。
