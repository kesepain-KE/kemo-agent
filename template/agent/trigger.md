# 注册信息

- **名称**: my_agent
- **触发**: 描述触发条件
- **职责**: 描述核心职责
- **模型**: cheap
- **工具**: 列出所需的插件和技能

# 操作信息

## 调用方式

由主智能体通过 `subagent_dispatch` 调用，`allowed_callers: ["main_agent"]`。

在此说明真实输入、输出、失败格式和调用限制。操作信息没有固定章节结构，
可以按子代理实际能力组织；额外实现文件由 `AGENT.md` 或可信执行入口按需引用。
