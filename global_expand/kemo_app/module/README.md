# Kemo App 用户配置面板

- 端口修改与设备 Token 替换会在受管桥接正在运行时安全停止旧实例，保存配置后再启动新实例。
- Token 只保存 SHA-256；面板只展示是否已配置及不可逆核对指纹，不返回明文。
- App 用户必须先通过原有密码管理流程创建，再绑定到一个已存在的 kemo-agent 用户目录。
- 绑定变更对下一次登录生效；已有短期会话保持登录时的智能体用户身份，直至重新登录。
- 本面板不修改 `input_data.md` 的数据注入开关，也不开放启动、停止、上游认证等其他配置。
