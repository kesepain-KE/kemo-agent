# Kemo 网关用户配置面板

- 面板只开放网关协议、IP/主机和端口，不开放 Provider、模型、密钥或网关管理操作。
- 已激活时，修改地址会沿用现有独立 `STATUS_TOKEN`，先验证新地址的 `/status`，成功后才替换配置。
- 尚未配置 Token 时，地址作为待激活地址保存；后续执行原有 `activate` 时可以直接复用。
- 面板配置不直接修改 `expand.json` 或数据注入开关。
