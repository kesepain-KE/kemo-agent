{工具日志压缩}
单轮对话工具调用压缩(最近的n1轮内的对话除外)
获取达到条件的单轮对话的对话数据(思考日志、工具调用日志)
总结成简短的工具调用日志(只保留并压缩关键思考、数据和调用)
替换原有的此轮对话的工具调用日志，并将思考日志替换为“已思考”

{上下文压缩}
上下文压缩(对话轮数达到n2轮时，触发对话压缩至n3轮，截断旧对话n2-n3轮，压缩至n3轮)
获取旧对话n2-n3轮的数据(只获取文本对话数据)
调用agents\self_improve，等待返回总结文本
替换掉旧的对话数据，将总结文本拼接到上下文中