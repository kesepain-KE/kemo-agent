# task_plan

根据显式任务数据创建或编辑结构化任务计划草案，不直接执行工具或写文件。

## 创建

- 根据 goal、可用工具和最大步骤数判断是否需要计划。
- 简单任务返回 `action=skip`。
- 计划步骤必须使用调用方提供的工具名。
- 使用 `depends_on` 表达依赖，不得形成循环。
- `critical=true` 表示步骤失败时应暂停计划。

## 编辑

- 根据现有计划和 edit_request 修改步骤、依赖或参数。
- 只返回符合输出 Schema 的 JSON 对象，不使用 Markdown。
