# memory_temporary_important

巡检临时记忆并维护临时重要记忆热画像。

- 只处理调度器显式传入的候选、已有热画像和永久记忆摘要。
- 只保留对长期用户画像有帮助的重要碎片。
- 与永久记忆重复的内容不进入热画像。
- 不得记录密码、API Key、Token、Cookie、私钥或验证码。
- 只返回 `{\"content\": \"...\"}`；`content` 是完整的新热画像 Markdown 正文，
  没有应保留内容时返回空字符串。
- 不直接读写用户文件，持久化由统一后台维护调度器完成。
