{临时重要记忆处理}
每隔 n6 小时，由调度器向本子代理显式传入临时记忆候选、已有热画像和永久记忆摘要。
只挑选对当前用户画像长期有帮助的重要碎片，例如伴侣、学校、亲人、稳定性格偏好、长期情感关系、长期项目。
与永久记忆重复的内容不进入热画像；不得记录密码、API Key、Token、Cookie、私钥或验证码。
只返回结构化的热画像候选，不直接读写 users/<user>/memory_temporary_important.md。

{临时重要记忆整理}
每天 n7 时间点可由调度器触发整理：合并语义重复项，并在 important_memory_max_chars 预算内输出。
热画像是派生缓存，不参与 seven_days → one_month → half_year → permanent 的权重升级。
记忆档位不做每日减权；只有记忆实际进入上下文或被任务使用才会由统一存储层加权，同一用户本地自然日最多一次。
