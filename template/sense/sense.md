# 感知模块名称

> 自动采集时间：待首次运行 data_update.py 填充

## 数据

模块尚未运行。等待 RuntimeHost 自动刷新，或手动运行 `python data_update.py` 初始化。

> 此出口只保存可共享的只读观测，不写入行为指令或隐私。真实采集后说明数据来源、采集时间、指标单位与必要的不确定性；失败不得把旧值写成刚刚采集的事实。格式自由，原始大数据保留在模块内，仅提供有界摘要。
