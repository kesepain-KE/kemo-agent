# Provider 工具参数恢复测试

本目录集中验证上游模型返回残缺工具 `arguments` 时的统一恢复行为，覆盖：

- Chat/Kemo 统一错误识别；
- 主智能体在已有正文或思考后的恢复；
- 并行工具调用整批确认，避免旧卡片和旧调用泄漏；
- 子代理与结构化输出工具使用相同的重试配置；
- 达到重试上限后明确失败且不执行残缺工具。

主运行时端到端场景还与 `tests/core/test_runtime_features.py` 中的历史、工具执行和流式事件断言交叉验证。
